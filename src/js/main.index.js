require.config({
    path: {
        jquery: "jquery",
        index: "index"
    },
    shim: {}
});
require(['jquery', 'index'], function($, index) {
    index.render();
    index.rende();
    index.rend();
    index.rent();
    index.kitchen();
    index.parts();
});
// require(['jquery', 'index'], function(jquery, index) {
//     index.rende()


// })